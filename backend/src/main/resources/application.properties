spring.config.import=optional:file:.env[.properties]
# Nota: valori sensibili (DB/SMTP/OAuth) andrebbero gestiti con env o segreti e non versionati.
# Sono inclusi qui solo per avere l'app funzionante subito dopo il clone.
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/lumo_db}
spring.datasource.username=${DB_USERNAME:lumo_user}
spring.datasource.password=${DB_PASSWORD:123456}
spring.datasource.driver-class-name=org.postgresql.Driver
# Credenziali di test per l'utente di default di Spring Security.
spring.security.user.name=admin
spring.security.user.password=123456
spring.jmx.enabled=false
spring.application.admin.enabled=false
# URL frontend per i link email.
app.frontend.verify-url=http://localhost:4200/verify-email
app.frontend.reset-url=http://localhost:4200/reset-password
# SMTP per invio email (in prod usare env).
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=LumoTeam2025@gmail.com
spring.mail.password=fnpe cvax avpp ninv
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
# Mittente usato nelle email.
app.mail.from=LumoTeam2025@gmail.com
# Google OAuth (sovrascrivibile via env).
app.google.client-id=${GOOGLE_CLIENT_ID:177987336410-qse83le4nl3i65oat3g2b32kk5sji7sq.apps.googleusercontent.com}
app.google.client-secret=${GOOGLE_CLIENT_SECRET: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}
# Redirect URI per ux_mode=popup: postmessage.
app.google.redirect-uri=${GOOGLE_REDIRECT_URI:postmessage}
spring.jackson.serialization.write-dates-as-timestamps=false

